#include <stdio.h>

int main() {
    float x, y;

    // Input the coordinates of the point
    printf("Enter the coordinates of the point (x y): ");
    scanf("%f %f", &x, &y);
if (x==0 &&y==0)
{printf("the point is att origin");

    /* code */
}
else if (x==0)
{printf("the point is at y axis");

    /* code */
}
else if (y==0)
{printf("the point is at x axis");
    /* code */
}
else {
    printf("the pointer is a t neither");
    
}




    return 0;
}
