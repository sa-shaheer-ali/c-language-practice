#include <stdio.h>
int main() {
    char alphabet = 'a';

    // print all alphabets from a to z
    while (alphabet <= 'z') {
        printf("%c ", alphabet);
        alphabet++;
    }

    return 0;
}
